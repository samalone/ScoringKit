.DS_Store
/.build
/Packages
/*.xcodeproj
xcuserdata/
DerivedData/
.swiftpm/config/registries.json
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
.netrc
build/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
.beads/dolt/

# Beads JSONL files are local-only in this project.
# issues.jsonl is a regenerable export (auto-export is off; use `bd export`).
# interactions.jsonl is an append-only local audit/recovery log — keep the file,
# just don't track it.
.beads/issues.jsonl
.beads/interactions.jsonl
